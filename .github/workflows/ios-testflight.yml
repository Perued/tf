name: Auto Sign And Upload TestFlight

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  upload-ipa:
    runs-on: ubuntu-latest

    steps:

      # Checkout repository with Git LFS
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          lfs: true

      # Debug repository files
      - name: Debug repository files
        run: |
          pwd
          echo "===== ROOT FILES ====="
          ls -lah

      # Restore signing certificate
      - name: Restore signing certificate
        run: |
          echo "${{ secrets.SIGNING_CERT_P12 }}" | base64 --decode > cert.p12

      # Restore provisioning profile
      - name: Restore provisioning profile
        run: |
          echo "${{ secrets.SIGNING_PROFILE }}" | base64 --decode > profile.mobileprovision

      # Restore App Store Connect API key
      - name: Restore App Store Connect API key
        run: |
          echo "${{ secrets.APP_STORE_CONNECT_API_KEY }}" | base64 --decode > AuthKey.p8

      # Install zsign
      - name: Install zsign
        run: |
          git clone https://github.com/zhlynn/zsign.git
          cd zsign/build
          chmod +x build.sh
          ./build.sh

          echo "===== FIND ZSIGN ====="
          find .. -name zsign

          sudo cp ../build/zsign /usr/local/bin/zsign || true
          sudo chmod +x /usr/local/bin/zsign

          zsign -v || true

      # Verify files
      - name: Verify files
        run: |
          echo "===== FILE CHECK ====="
          ls -lah
          file app.ipa || true
          file cert.p12 || true
          file profile.mobileprovision || true
          file AuthKey.p8 || true

      # Re-sign IPA
      - name: Sign IPA
        run: |
          zsign \
            -k cert.p12 \
            -p "${{ secrets.SIGNING_CERT_P12_PASSWORD }}" \
            -m profile.mobileprovision \
            -o signed.ipa \
            app.ipa

      # Verify signed IPA
      - name: Verify signed IPA
        run: |
          echo "===== SIGNED IPA ====="
          ls -lah signed.ipa

      # Upload to TestFlight
      - name: Upload to TestFlight
        run: |
          xcrun iTMSTransporter \
            -m upload \
            -assetFile signed.ipa \
            -apiKey "${{ secrets.ASC_KEY_ID }}" \
            -apiIssuer "${{ secrets.ASC_ISSUER_ID }}"

      # Upload artifact
      - name: Upload signed IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: signed-ipa
          path: signed.ipa
